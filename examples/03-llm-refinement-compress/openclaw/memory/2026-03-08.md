Guided a startup through their first production deployment on AWS.
They were running everything on a single EC2 instance. Recommended
splitting into ECS Fargate for the API and RDS for the database.
Estimated monthly cost: ~$150 for their current traffic level, with
auto-scaling configured to handle 10x spikes.

Revisited the EKS conversation from last week. The team decided to
go with EKS after all. Helped them set up a basic cluster with
managed node groups and cluster autoscaler. Pointed them to the
EKS best practices guide for networking and IAM configuration.
