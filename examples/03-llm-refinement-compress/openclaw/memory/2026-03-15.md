Another EKS question this week — a different team considering EKS vs
GKE for a multi-cloud setup. Recommended GKE Autopilot as the primary
cluster since it reduces node management overhead, with EKS as the
secondary. Kubernetes configs should transfer cleanly between them.

Spent time on a cost optimization review. The user was running m5.xlarge
instances 24/7 for a workload that only peaks during business hours.
Recommended Savings Plans + scheduled scaling. Estimated 40% cost
reduction.
