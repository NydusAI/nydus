# Configuration (`pynydus.config`)

Environment-based settings: `load_config()`, `NydusConfig`, and `RegistryConfig`.

- **User guide:** {doc}`../configuration` (environment variables)
- **Full module API (auto-generated):** {doc}`../apidocs/pynydus/pynydus.config`
